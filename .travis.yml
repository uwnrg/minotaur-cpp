 language: cpp

 compiler: gcc

 before_install:
  - sudo apt-get update
  - sudo apt-get install qt5-default qttools5-dev-tools

 before_script:
   - mkdir build
   - cd build
   - qmake -o makefile ..\minotaur.pro

 script: make
